import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'com.blindsbook.draperycalculator',
  webDir: 'www'
};

export default config;
